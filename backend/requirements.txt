# --- Spletni strežnik / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Relacijska baza (MySQL) ---
SQLAlchemy==2.0.36
PyMySQL==1.1.1
cryptography==44.0.0

# --- Vektorska baza ---
qdrant-client==1.12.1

# --- Generativna UI / embeddingi ---
openai==1.59.6

# --- Zajem podatkov (web scraping) ---
scrapy==2.12.0
# Twisted mora biti < 24.7.0: novejše verzije so odstranile interno funkcijo
# `_setAcceptableProtocols`, ki jo Scrapy 2.12 še uvaža (sicer ImportError ob zagonu).
Twisted==24.3.0
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Pomožno ---
python-dotenv==1.0.1
tenacity==9.0.0
